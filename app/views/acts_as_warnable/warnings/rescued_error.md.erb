Software error: <%= error.class.name %>
------------------------

##### <%= error.message %>

This is either a bug or a server error.
**If you don't know why you got this, contact devteam before dismissing.**

## Technical details

<% error.backtrace.each do |filename| %>
* <%= filename.gsub("'", "`").html_safe %>
<% end %>

<% if (response = inspection[:response]) %>
### Response

<% if code = inspection[:code] %>
<%= code %>
<% end %>

<% if response.is_a?(Hash) %>
``` json
<%= JSON.pretty_generate(response) %>
```
<% elsif response.respond_to?(:to_s) %>
<%= response.to_s.truncate(10000) %>
<% end %>
<% end %>
